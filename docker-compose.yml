services:
  api:
    build: .
    container_name: pare-hurip-api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    mem_limit: 3g
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
